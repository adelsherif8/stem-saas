# Optional extras for REAL stem separation (DEMUCS_REAL=1).
# Heavy (~2 GB: pulls in PyTorch). Install ON TOP of requirements.txt:
#     pip install -r requirements.txt -r requirements-ml.txt
#
# demucs pulls torch + torchaudio; torchcodec is what torchaudio uses to WRITE
# the stem files on recent versions (without it, saving fails). Needs ffmpeg
# available on the system (brew install ffmpeg / apt-get install ffmpeg).
demucs==4.0.1
torchcodec
